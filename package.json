{
  "name": "7-nights",
  "private": true,
  "description": "7 Nights — a survival sandbox built with Three.js. Static site; no build step.",
  "type": "module",
  "scripts": {
    "test": "node --import ./tests/setup.js --test"
  },
  "engines": {
    "node": ">=20.6"
  }
}
